# Next.js

Detect `next.config.*` with `app/` or `pages/`. Use root layouts, pages,
global CSS imports, CSS modules, and dynamic imports to identify production
and route/lazy roots. Treat Storybook separately. Verify modes from rendered
or built route output when static declarations cannot prove the final value.
