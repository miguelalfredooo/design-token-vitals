# Vite

Detect `vite.config.*` and HTML or `src/main.*` entries. Resolve configured
aliases and CSS/JS imports from each entry. Mark generated `dist/` as output,
not a consumer source; use it only for build verification when available.
