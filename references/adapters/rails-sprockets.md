# Rails / Sprockets

Detect `Gemfile` with `app/assets/stylesheets/`. Start from manifest and
application bundle registrations, including engine assets. Trace Sass and
Sprockets directives; distinguish gem/vendor assets from app-owned sources.
Inspect compiled assets or a running development build before claiming theme
or mode parity.
