# Monorepo

Detect workspaces or `turbo.json`. Identify every application candidate and
ask the user to select one when several production apps would materially
change scope. Resolve workspace-package imports before classifying a source
as external or orphaned.
